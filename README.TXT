========================

To run the project from the command line, go to the current folder and
type the following:

java -jar "PasswordPageMonitor.jar" 


Before running you need to edit Config, and insert some proxies (or run proxyless).
